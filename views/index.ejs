<html lang="en">
<%- include("./partials/head.ejs") %>

<body>
  <%- include("./partials/nav.ejs") %>

  <div class="blogs content">
    <h2>Todas las tortugas</h2>
  <ol>
    <% if (turtles.length > 0) { %>
      <% turtles.forEach(turtle => { %>

        <li>La tortuga <%= turtle.name %> es del tipo <%= turtle.type %>.</li>

      <% }) %>
    <% } else { %>
      <p>¡No hay tortugas!</p>
    <% } %>
  </ol>
  </div>

  <%- include("./partials/footer.ejs") %>
</body>
</html>