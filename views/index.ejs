<html lang="en">
<%- include("./partials/head.ejs") %>

<body>
  <%- include("./partials/nav.ejs") %>

  <div class="blogs content">
    <h2>Todas las tortugas</h2>

    <% if (turtles.length > 0) { %>
      <% turtles.forEach(blog => { %>

        <h3 class="title"><%= turtles.name %></h3>
        <p class="snippet"><%= turtles.type %></p>

      <% }) %>
    <% } else { %>
      <p>¡No hay tortugas!</p>
    <% } %>
    
  </div>

  <%- include("./partials/footer.ejs") %>
</body>
</html>